import styled from "styled-components";

/*
  <Container> have to be used inside pages to ensure correct margins and paddings
*/

const Container = styled.div`
  padding: 10px 20px;
  display: flex;
  flex: 1;
  flex-direction: column;
`;

export default Container;
