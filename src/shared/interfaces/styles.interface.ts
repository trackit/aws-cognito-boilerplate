/**
 * Used for theming
 */
export interface Colors {
  primary: string;
  secondary: string;
  primaryTextColor: string;
  secondaryTextColor: string;
  background: string;
  default: string;
  success: string;
  warning: string;
  danger: string;
}
